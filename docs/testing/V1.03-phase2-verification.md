# V1.03 阶段二验证记录：搜索、收藏、最近播放和歌单

测试日期：2026-09-23
设备：vivo V2520A（Android 16 / API 36）
工具链：JDK 17、Android SDK 36（Build Tools 36.0.0）、Gradle 9.6.0

## 本阶段改动

### 1. 歌单顺序（原有实现的缺陷）

V1.02 的 `TrackGroup.trackUris` 是 `Set<String>`，集合不保留插入顺序，因此“歌单播放顺序”在旧实现下无法保证。
现改为 `List<String>`：

- 保存顺序即播放顺序，新增歌曲追加到末尾（`addTracksToGroup`），不覆盖已有顺序。
- 读取时去重但**不重新排序**，所以 V1.02 用户已有的分组不会被打乱。
- `LibraryQuery.resolve` 按保存顺序解析可用歌曲，音频被移动/删除的条目被跳过而不是中断播放。

### 2. 搜索

- 匹配范围从「歌名 / 歌手 / 文件名」扩展到**专辑**。
- 多关键词以空格分隔，采用 AND 语义：`稻香 DEER` 收窄结果而不是放宽。
- 大小写不敏感，命中文字在列表中加粗高亮。
- 逻辑抽到纯 Kotlin 的 `LibraryQuery`，便于单元测试。

### 3. 收藏

- 新增「收藏」标签页，心形按钮在歌曲列表、分组、专辑/艺术家列表均可用。
- 按添加顺序保存并整列播放；音频不可访问时自动跳过。
- 存储在 `favorite_track_uris`。

### 4. 最近播放

- **由播放服务写入**（`PlaybackService.recordPlayed`），因此在通知栏、桌面小部件或恢复队列触发的播放同样会被记录。
- 列表只展示当前仍可访问的本地歌曲，最新在前。
- 历史以 `uri<TAB>时间戳` 文本保存，解析失败的行被丢弃而不抛异常；上限 200 条。
- 不可访问的条目**保留在存储中**，避免存储卡临时卸载时清空用户历史。

### 5. 备份与恢复

- 备份 schema 升级到 2，新增 `favoriteTrackUris` 与 `playHistory`。
- **兼容 V1.02 的 schema 1 备份**：仍可导入。
- 缺少这两个字段的旧备份导入时，会保留现有收藏与历史，而不是清空。

## 自动化验证结果

### 单元测试 ✅

```text
gradlew :app:testDebugUnitTest
```

10 个测试类、**50 个测试全部通过**（本阶段新增 19 个）：

| 测试类 | 用例数 | 覆盖 |
| --- | --- | --- |
| `PlayHistoryTest` | 7 | 编解码往返、损坏行容错、去重置顶、上限裁剪、不可访问过滤 |
| `FavoriteTracksTest` | 4 | 增删切换、顺序保持、空值、不可访问过滤 |
| `LibraryQueryTest` | 8 | 空查询、四字段匹配、大小写、多词收窄、歌单顺序、缺失跳过、去重 |
| 其余 7 个既有测试类 | 31 | 未回归 |

### Lint ✅

```text
gradlew :app:lintDebug
```

`BUILD SUCCESSFUL`，无新增 lint 问题。基线内 3 条 `UseKtx`/`ApplySharedPref` 提示为既有 `PlaybackPreferences` 写法，仅因行号位移重新出现。

### 构建 ✅

```text
gradlew :app:assembleDebug
```

`BUILD SUCCESSFUL`。

## 真机验证

### 已完成

- 设备 `V2520A`（Android 16 / API 36）通过 ADB 正常连接。
- 确认设备上仍装有 V1.02 正式包（`com.imankoppai.mediaanvil`，`versionCode=3`、`versionName=1.02`），可作为第八阶段升级测试的基线。

### 未完成（阻塞原因）

### 已完成（设备解锁后补做）

设备：vivo V2520A（Android 16 / API 36），debug 包 `com.imankoppai.mediaanvil.debug`。

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 收藏可添加并显示 | ✅ | 点歌曲行爱心后，该曲进入「收藏」页，按钮变为「取消收藏」 |
| **重启后收藏仍在** | ✅ | `am force-stop` 后重新启动，爱心仍为实心选中状态 |
| **播放记录由播放服务写入** | ✅ | 设备端 `mediaanvil_playback.xml` 出现 `play_history`，格式为 `uri<TAB>时间戳`；「近期」页显示该曲 |
| 播放正常 | ✅ | 媒体会话 `position=3003`（3 秒曲目播完），`error=null`，无 `FATAL EXCEPTION` |
| 搜索匹配文件名 | ✅ | 搜索 `tagtest` 精确筛出文件名为 `dsh_tagtest.mp3` 的单首歌曲（其内嵌标题不同），验证了阶段二新增的匹配逻辑 |

标签页文案后续按需求调整为 **列表 / 收藏 / 近期 / 歌单**（原为「最近播放」「分组」），已在真机确认。

### 未完成（说明）

以下两项未做真机手工确认，但已有自动化覆盖：

- [ ] 移动或删除音频后不崩溃，且该歌曲从收藏/近期中消失
- [ ] 收藏列表与歌单的播放顺序与界面显示一致

`Phase2PersistenceTest`（androidTest）使用真实的设备端 `SharedPreferences`，并以新建实例模拟应用重启，
覆盖持久化契约；该测试由 CI 的 `connectedDebugAndroidTest`（模拟器 API 35）执行并通过。

## 结论

代码、57 个单元测试、Lint（基线未改动）与 Debug 构建均通过；
歌单顺序这一验收项从「无法保证」变为「有测试保证」。
真机已验证收藏跨重启保留、播放记录由服务写入、搜索匹配文件名与播放正常。
